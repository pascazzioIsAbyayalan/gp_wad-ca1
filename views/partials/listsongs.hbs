{{#if singleGame.games}}

  <table class="ui unstackable striped table">
    <thead>
      <tr>
        <th>Game</th>
        <th>Developer</th>
      </tr>
    </thead>
    <tbody>
      {{#each singleGame.games}}
        <tr>
          <td>
            {{title}}
          </td>
          <td>
            {{developer}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  {{else}}
    <h3 class="ui message">
      Empty playlist!
    </h3>

{{/if}}
