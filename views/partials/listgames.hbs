{{#if games}}

  <table class="ui unstackable striped table">
    <thead>
      <tr>
        <th>Game</th>
        <th>Developer</th>
      </tr>
    </thead>
    <tbody>
      {{#each games}}
        <tr>
          <td>
            {{title}}
          </td>
          <td>
            {{developer}}
          </td>
          <td class="center aligned">
    <a href="/playlist/{{../id}}/deletegame/{{id}}" 
	        title="Delete game">
            <i class="ui black trash icon"></i>
    </a>
</td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  {{else}}
    <h3 class="ui message">
      Empty list!
    </h3>

{{/if}}
